[package]
name = "splitway-daemon"
version = "0.0.3"
edition = "2021"

[dependencies]
splitway-shared = { path = "../splitway-shared" }

serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4.20"
env_logger = "0.11"
