# Frontend build + deps (rebuilt locally; not committed — packaging is Phase 7d).
ui/node_modules/
ui/dist/

# Tauri-generated ACL/permission schemas (regenerated by build.rs from
# tauri.conf.json + capabilities/).
gen/
