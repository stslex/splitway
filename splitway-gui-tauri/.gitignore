# Frontend build + deps (rebuilt locally; not committed — packaging is Phase 7d).
ui/node_modules/
ui/dist/

# Tauri-generated ACL/permission schemas (regenerated by build.rs from
# tauri.conf.json + capabilities/).
gen/

# macOS bundle staging: the helper binaries + bootstrap + plist are copied here
# by scripts/build-macos-app.sh and embedded as app Resources. Build output, not
# source (bootstrap.sh + the plist are tracked at their real homes; only their
# staged copies + the release binaries land here). See docs/design/macos-self-install.md.
resources/
